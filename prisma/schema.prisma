generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("COCKROACH_URI")
}

model MiniMartProduct {
  id        String   @id @default(cuid())
  title     String
  price     Float
  createdAt DateTime @default(now())
}