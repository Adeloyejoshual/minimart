model MinimartProduct {
  id          Int      @id @default(autoincrement())
  title       String
  description String?
  price       Float
  image_url   String?
  seller_id   Int
  seller_name String
  created_at  DateTime @default(now())
}