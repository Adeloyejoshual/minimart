generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
}

// Products table
model Product {
  id    Int    @id @default(autoincrement())
  name  String
  price String
}